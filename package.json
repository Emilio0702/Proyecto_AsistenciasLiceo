{
  "name": "serviterra-registro-colacion",
  "version": "1.0.0",
  "description": "Sistema de Registro de Colaciones - ServiTerra",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "docker-up": "docker-compose up -d",
    "docker-down": "docker-compose down",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "start": "concurrently --kill-others \"npm run backend\" \"npm run frontend\"",
    "start-full": "concurrently --kill-others \"npm run docker-up\" \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
